model:
  name: "gpt-4o-mini"
  temperature: 0.7
  max_retries: 3
  retry_delay: 2

data:
  min_user_interactions: 5
  max_users: 50
  synthetic_sample_size: 20

simulation:
  num_creators: 5
  num_rounds: 5
  reward_mechanism: "default"

content:
  title_word_limit: 15
  abstract_word_limit: 100
